version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/app
hooks:
  AfterInstall:
    - location: scripts/install.sh
      timeout: 30
      runas: ubuntu
  ApplicationStart:
    - location: scripts/start.sh
      timeout: 30
      runas: ubuntu
